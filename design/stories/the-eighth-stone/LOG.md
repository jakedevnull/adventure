# Orchestration log — The Eighth Stone

Mirror of the Linear document linked to JAK-38 (the primary copy).

## 2026-09-17 17:04Z · step 1 · START

- event:    orchestrator session opened on JAK-38 (label `Game Story`), story branch `cylocal1/jak-38-the-eighth-stone`
- saw:      factory block complete — max_rooms: 8, max_rounds: 3, round: 0, max_revisions: 5, revisions: 0, evaluate: true. Slug `the-eighth-stone`. Worktree clean at 417ba7d; fourteen rooms already exist (six turning-house faces, six crossroads-yard faces, two high-crossing faces).
- decision: proceed straight to §1 Plan; nothing written back to the issue.
- reason:   every key is present in the `factory:` block, so there are no defaults to fill in; `evaluate: true` means the full loop runs through §5.
- action:   read ADVENTURE_FACTORY.md and the three design docs; read `src/content/index.ts`, `high-crossing-2099-ba.ts` and `src/world.ts` to get the engine facts the planner will need; pushed the story branch to origin; created this log.
- next:     create the Plan sub-issue.

## 2026-09-17 17:05Z · step 2 · SPAWN Plan (JAK-39)

- event:    sub-issue created — JAK-39 "Plan: The Eighth Stone", label `Plan`, project `adventure`, state Todo, assignee jakedevnull, delegate cylocal.
- saw:      the story asks for a new place, the ford, east of `high-crossing`, persisting across at least four ages, with ELLERMARK / EIGHT as the through-line and the river as the edge of the map.
- decision: pass the story verbatim and add four engine facts of my own — the strideless `high-crossing` faces and their single `west` exit, the `MILESTONE_LETTERS` constant, `strideTarget`'s adjacent-landing-only rule, and the room budget counting new rooms only. Added a fourth acceptance criterion requiring the plan to state each face's exits and time flags and to walk the reachability chain on paper.
- reason:   the outline is the generator's only source (ADVENTURE_FACTORY.md §5.3), and this story's one real trap is temporal: a face that opens FUTURE into a landing with no face gives the player a refusal instead of a room, and the story explicitly asks for ages the crossing does not exist in. Making the planner write the flags down is where that trap gets caught cheaply, before eight rooms are written against it. The `MILESTONE_LETTERS` pointer exists because "the same hand" is a claim the code can enforce; the previous story already proved it works.
- action:   JAK-39 created and delegated (spawns the child session); JAK-38 marked blocked-by JAK-39; deadline wakeup set 20m.
- next:     the planner's completion, or the 20-minute deadline check — whichever comes first.

## 2026-09-17 17:15Z · step 3 · PLAN ACCEPTED (JAK-39) · CLOSE-OUT

- event:    the planner completed; OUTLINE.md (520 lines) landed on PR #7 against the story branch.
- saw:      8 room lines of 8 allowed — six `ford` faces and two `east-road` faces. Four required headings present; Blockers `_None._`. Two judgement calls flagged rather than buried: the ford takes all six landings (arithmetic, not appetite — a stride only reaches the adjacent landing, so 1099 BA, 99 BA, 99 AA are the chain that gets the player from a ford they can walk to in 2099 BA to the Rekindling lock), and the new `east` exit at both crossing faces is undiscoverable in the prose, which the planner recorded and did not act on.
- decision: accept.
- reason:   the count is exactly at `max_rooms`; every line carries place, landing, age and purpose; the Story section quotes the author verbatim then expands it into beats, a per-face exits-and-flags table with a reason column, the eighth stone age by age, the refusal per age for the far bank, and a build order arranged so every commit leaves the world valid. The all-six-faces call is the author's own requirement ("the years run from the ford as they run from the yard") turned into the only shape the engine allows, and the two extra faces do real work rather than filling budget. The discoverability note is correct to leave alone: the issue forbids changing anything in those two files but the exit, and both `look` bodies are already at the voice lint's four-sentence ceiling, so there is no legal edit — it is a human's call in a separate issue.
- action:   merged origin/cylocal1/jak-39-plan-the-eighth-stone into the story branch (3af30e3) and verified there: typecheck silent, `npm test` 46 pass / 0 fail, `eval:reach` 14 of 14 — the merge touches one file and no game content. Spot-checked four claims the plan rests on: `high-crossing:99-aa` has `west` only, the id-collision list is real (`milestone-1099-aa`, `tolls` both exist), the engine's no-exit reply is the fixed `You can't go that way.` (`src/engine.ts:223`), and `OUT`, `EAST` lands at `high-crossing:2099-ba` through the real engine. Story branch pushed. JAK-39 closed out: 5/5 criteria ticked, state Done, blocked-by removed.
- next:     spawn the generator.

## 2026-09-17 17:16Z · step 4 · SPAWN Generate (JAK-40)

- event:    sub-issue created — JAK-40 "Generate: The Eighth Stone", label `Generate`, project `adventure`, state Todo, assignee jakedevnull, delegate cylocal.
- saw:      eight unchecked room lines, a build order the plan has already worked out, and two one-line edits to shipped rooms.
- decision: add three acceptance criteria the outline implies but does not gate — the final `eval:reach` total (22 of 22), the existing-room edits staying to two exit lines with anything else declared, and the eighth stone's letters being one exported constant imported five times. Deliberately did **not** ask the generator to run `eval:voice` or an EXAMINE sweep.
- reason:   the first two are the cheapest way to check the two things this story can quietly get wrong — a face stranded off the chain, and shipped prose from the last story edited under cover of this PR — and both are checkable in a diff and a harness rather than by reading. The third makes "the same hand" a fact about the module graph, as `MILESTONE_LETTERS` already is. The voice lint and the noun sweep are withheld on purpose: they are the evaluator's job, and a generator that pre-runs them turns the first evaluation into a rubber stamp (ADVENTURE_FACTORY.md, and the skill's "leave the evaluator something to find").
- action:   JAK-40 created and delegated; JAK-38 blocked-by swapped from JAK-39 to JAK-40; deadline wakeup set 20m.
- next:     the generator's completion, or the 20-minute deadline check.

## 2026-09-17 17:30Z · step 5 · ROOMS ACCEPTED (JAK-40) · CLOSE-OUT

- event:    the generator completed all eight rooms in one session; PR #8 against the story branch.
- saw:      on the merged story branch: typecheck silent, 46 pass / 0 fail, `eval:reach` verdict PASS at 22 of 22. All eight `## Rooms` lines ticked with as-built notes, zero unticked, Blockers still `_None._`. One declared addition beyond the plan: a `box` scenery in `ford:2099-ba`.
- decision: accept, merge, and tick all seven criteria.
- reason:   every claim re-ran green under me rather than being taken on report. The diff against the story branch touches the two shipped `high-crossing` files by exactly `+ east: "east-road:2099-ba"` and `+ east: "east-road:99-aa"` with no deletions, which is the criterion that protects the previous story's evaluated prose. `EIGHTH_STONE_LETTERS` is one `export const` at `ford-2099-ba.ts:25` and five imports, and `grep -rno "EIGHT\b" src/` returns that one line — so byte-identity across six ages is a fact about the module graph and the word appears nowhere a player can reach except on the stone. The declared `box` scenery is the writing guide's Rule 9 applied correctly: both the `look` and the tollman's `talk` name the box, so it has to answer to EXAMINE.
- action:   merged origin/cylocal1/jak-40-generate-the-eighth-stone (f464266) and verified on the story branch. Played all nine per-room routes plus the full there-and-back route (`OUT` · three `EAST` · five `FUTURE` · five `PAST` · three `WEST` · `IN`): ten of ten exit 0. Played the ford in 2099 BA by hand — `READ STONE` returns `ELLERMARK` / `EIGHT`, and `EAST` at the river returns `You can't go that way.`, so the edge of the map holds. Read the Lettered Age label at `ford:2099-aa`: it dates the stone by the lock wall it came out of, calls the second line an uninterpreted mason's tally, and never mentions the crossing or a sequence. Story branch pushed. JAK-40 closed out: 7/7 ticked, state Done, blocked-by removed.
- next:     spawn the evaluator.

## 2026-09-17 17:31Z · step 6 · SPAWN Evaluate (JAK-41)

- event:    sub-issue created — JAK-41 "Evaluate: The Eighth Stone", label `Evaluate`, project `adventure`, state Todo, assignee jakedevnull, delegate cylocal. Round 0.
- saw:      eight rooms that pass reachability and play, and whose voice lint and EXAMINE sweep nobody has run.
- decision: spawn the evaluator and tell it plainly that the voice lint and the noun sweep are unrun, then point it at four things the harnesses cannot see: the stone's letters identical in six ages with the word "eight" nowhere else; the label and the University placard not knowing about each other; no `east` exit and an in-voice far-bank refusal at all six ford faces; and the two strideless road faces printing no time line.
- reason:   `evaluate: true`, so the loop runs (ADVENTURE_FACTORY.md §5.6). This is the first evaluation that is a genuine first look rather than a confirmation — the generator was deliberately not asked to pre-run either check — so findings here are the loop working, not a defect. The four pointers are the failures that would pass both harnesses green and still break the story: the payload of this story is a connection the text refuses to make, and only a reader can confirm a refusal. Repeated the `--expect` warning because it produced false FAILs on the previous story.
- action:   JAK-41 created and delegated; JAK-38 blocked-by swapped from JAK-40 to JAK-41; deadline wakeup set 15m.
- next:     the evaluator's verdict, or the 15-minute deadline check.

## 2026-09-17 17:40Z · step 7 · EVALUATION FAIL (JAK-41) · ROUND 0 → 1 · CLOSE-OUT

- event:    the evaluator reported `verdict: FAIL` at round 0 — 22 of 22 reachable, 0 voice findings, 9 nouns missing.
- saw:      both harnesses clean and all four of the issue's targeted checks passing on evidence rather than assertion: the stone reads byte-identical in six ages from one constant and five imports; the label and the University placard never name each other (checked by absence — no "crossing", "sequence", "nine" in one, no "ford", "lock", "towpath" in the other), while both carry the University's mark, so one institution holds two incompatible wrong readings a mile apart; no ford face has an `east` exit and all six refuse the far bank in their own age's terms; both road faces print no time line and no route reaches "nothing of this place stands in that age". The FAIL rests entirely on the EXAMINE sweep — 113 calls, 9 misses, the worst being `EXAMINE COURSE` at `ford:1099-aa`, where the `look` names the eighth stone as "one course of it" and that exact word does not resolve.
- decision: accept the FAIL as correct, increment round 0 → 1, and send the report back to the generator's existing session with a scoped fix list of twelve nouns — the nine, plus the three the evaluator resolved by synonym and did not count (`waterline` twice, `banks`). Overruled the evaluator's suggestion that `knees`, `foot` and `arm` might be out of scope. Ruled the shipped `high-crossing` version of that gap out of scope and kept every word of prose frozen.
- reason:   I re-ran the deciding claims myself before acting: both harnesses green, and `EXAMINE COURSE`, `TOLL-BOARD`, `WALL`, `CUTTING`, `KNEES` all return "You can't see any such thing." through the engine. The findings are real and the verdict is right. On the sub-parts: the evaluator is correct that `EXAMINE FOOT` also misses at the shipped `high-crossing:2099-ba` — I confirmed it — but a pre-existing gap elsewhere does not make a new room's miss acceptable, and the fix is one word in an array the room already has. The shipped file stays untouched because editing another story's evaluated prose under cover of this PR is the human's call (ADVENTURE_FACTORY.md §6.1), so it goes on the PR as a note instead. The three synonym near-misses are added because they cost the same one word and the guide's Rule 9 does not have a "near enough" clause. Prose is frozen because the voice lint is at 0 and the read-aloud judgement was good: every sentence reopened is a finding risked for nothing. Fixing through the existing generator session keeps its worktree, its context and its PR.
- action:   `round: 0` → `round: 1` in the story issue's `factory:` block. Full report plus the twelve-noun fix list sent to the generator session (`linear_agent_give_feedback`), with the four commands it must re-run and an instruction to play every fixed noun through the engine and paste the reply — a `nouns` entry that does not resolve is this round's entire failure mode, so a claim is not enough. JAK-41 closed out: 6/6 criteria ticked, state Done. JAK-40 reopened to In Progress and JAK-38 blocked-by swapped back to it. Deadline wakeup set 15m.
- next:     the generator's fix, or the 15-minute deadline check. Rounds used: 1 of 3.

## 2026-09-17 17:58Z · step 8 · DEADLINE FIRED · RE-PROMPT DEAD · SPAWN Generate (round 1) (JAK-42)

- event:    the 15-minute deadline on the fix round fired with nothing received from the generator.
- saw:      JAK-40 In Progress since I reopened it, but `origin/cylocal1/jak-40-generate-the-eighth-stone` is 0 commits ahead of the story branch — nothing pushed in the 17 minutes since the feedback — and `/Users/jakelevirne/.cyrus/worktrees/JAK-40` **does not exist**. No replacement worktree anywhere: only JAK-36, JAK-38 and JAK-41 remain. The `linear_agent_give_feedback` call returned `success: true`, so Linear accepted the message; the session had no working directory to act on it in.
- decision: stop waiting and create a fresh fix-round child, JAK-42 "Generate (round 1): twelve nouns that do not answer to EXAMINE", carrying the whole fix list in its description. Left JAK-40 at Done rather than Canceled.
- reason:   §8 says never act on guesswork and wait again if the child is visibly progressing — it is not: no commits, no worktree, no session output. §6 anticipates exactly this and prescribes a new `Generate (round <n>)` sub-issue when the generator's session is gone. **The cause is my own close-out:** moving JAK-40 to Done is what tore its worktree down (§10 — "Done also tears down its worktree"), and the evaluator only reports afterwards, so by the time §6's "re-prompt the existing generator session" is reachable, the session it names is always already dismantled. That is a contradiction in the factory's own procedure, not a one-off; it belongs on the PR as a note for the human. JAK-40 stays Done because its eight rooms were verified and merged — it was not superseded, and nothing about its work is in question.
- action:   JAK-42 created and delegated, with the twelve nouns named one by one (the nine from the report plus `waterline` twice and `banks`), the exact scenery/`nouns` target for each, an explicit do-not-touch list (the two `high-crossing` files, every yard and House file, all prose, the four `talk`/`read`-only nouns, `EXAMINE BOAT`), and a requirement to paste the engine's reply for each of the twelve rather than claim the fix. Round stays 1 — this is the same fix round re-issued, not a new one. JAK-40 set back to Done; JAK-38 blocked-by swapped to JAK-42. Deadline wakeup set 15m.
- next:     JAK-42's completion, or the 15-minute deadline check. Rounds used: 1 of 3.

## 2026-09-17 18:04Z · step 9 · ROUND 1 FIXES ACCEPTED (JAK-42) · CLOSE-OUT · SPAWN Evaluate (round 1) (JAK-43)

- event:    the fix-round child completed; PR #9 against the story branch. It reported 6 of 7 criteria met and one it could not execute.
- saw:      on the merged story branch (5c24d99): typecheck clean, 46 pass / 0 fail, `eval:reach` PASS 22 of 22, and `eval:voice -- --story the-eighth-stone` PASS with 0 findings. All twelve nouns answer through the real engine, played by me one room at a time — including `EXAMINE COURSE` at `ford:1099-aa`, the miss that decided round 0, which now returns the stone. `EXAMINE CHAMBER WALL` at `ford:2099-aa` returns the new wall instead of the gates, and `EXAMINE CHAMBER` still returns the gates, so the widening did not swallow anything. The whole `src/content` diff outside `nouns:` arrays is a single four-line scenery block. The child also declared the one criterion it could not run.
- decision: accept, merge, and tick all seven criteria including the `eval:voice` one the child marked ✗.
- reason:   the criterion the child could not tick was a criterion I should not have set: the voice lint is denied to a `Generate` session by the label's tool preset and by the `generate-story` skill, which is the same rule I invoked two steps earlier when I deliberately withheld it — so I asked for something the role is built not to do. The child did the right thing: it refused to work around the permission, hand-checked its one new lint-scope string against each rule in `src/voice.ts`, and declared the gap rather than claiming a pass. I ran the lint myself and it passes with 0 findings, so the tick is a verification record in my hand, which is what the ticks are for. The rest is accepted because I re-played every one of the twelve nouns and read the diff rather than the summary.
- action:   merged origin/cylocal1/jak-42-… (5c24d99) and verified on the story branch; story branch pushed. JAK-42 closed out: 7/7 ticked, with the `eval:voice` tick annotated on the issue to record who actually ran it; state Done. JAK-43 "Evaluate (round 1): The Eighth Stone" created and delegated; JAK-38 blocked-by swapped to it; deadline wakeup set 15m.
- next:     the round-1 verdict, or the 15-minute deadline check. Rounds used: 1 of 3.

Note for the human, to go on the PR: my fix-round sub-issue asked the generator to run `npm run eval:voice`, which its role is configured to refuse. ADVENTURE_FACTORY.md §11 reserves that command for the evaluator on purpose; the orchestrator skill should say plainly that a fix round's voice-lint check belongs to the orchestrator or the next evaluator, never to the generator's own criteria.

## 2026-09-17 18:12Z · step 10 · EVALUATION FAIL (JAK-43) · ROUND 1 → 2 · CLOSE-OUT · SPAWN Generate (round 2) (JAK-44)

- event:    the round-1 evaluator reported `verdict: FAIL` — 22 of 22 reachable, 0 voice findings, 4 noun findings (3 missing, 1 answering with the wrong thing).
- saw:      round 1's twelve nouns all answer, and the regression hunt came back empty on evidence rather than sampling: the evaluator drove the real `Game` down every route and typed EXAMINE for all 402 nouns declared on the story's 69 examinables, and every one resolves to its own entry — six widened arrays, nothing shadowed. It also verified the "no prose changed" claim from the diff itself and defended the one new scenery sentence on craft grounds. The four new findings are `foot` at `ford:99-ba`, `face` and `stone chamber` at `ford:1099-aa`, `end` at `ford:99-aa`. I confirmed all four through the engine: three return "You can't see any such thing.", and `EXAMINE STONE CHAMBER` returns the milestone while `EXAMINE CHAMBER` correctly returns the gates.
- decision: accept the FAIL, increment round 1 → 2, and scope the next fix round to the **class** rather than the four findings — a sweep of two named patterns across all eight rooms, with the findings reported including the instances that already answered.
- reason:   the evaluator's diagnosis is the important part of its report and it is right: these are not four new defects but one unfinished fix. Round 1 repaired `knees` and `foot` at `ford:2099-ba`, wrote the principle into the outline — "Possessive sub-parts: the room names them, so they answer" — and never applied it to the other seven rooms; `stone chamber` is the same wrong-thing shape round 0 caught at `ford:2099-aa`, one room upstream. A third list-shaped fix would leave a fourth reader a fifth noun to find, and with `round: 2` of 3 that path ends in escalation over nouns. So the sub-issue asks for the sweep first and the four second, and requires the already-answering instances to be reported — that is the only way I can tell a sweep from a list. I also told it explicitly **not** to run `eval:voice`, correcting my own round-1 mistake.
- action:   `round: 1` → `round: 2`. JAK-44 "Generate (round 2): sweep the whole noun class, not the list" created and delegated, carrying the two patterns (`its <noun>` / `the <noun> of it` / `<noun> and all`; multi-word phrases no entry declares exactly), the four confirmed findings, the unchanged prose freeze and out-of-scope file list, and the instruction to declare what it judges not to be a thing. JAK-43 closed out 6/6 — including the sweep criterion it marked ✗ itself, which I ticked: the criterion asks for the sweep and the misses, and it delivered both; an unclean sweep is the story's FAIL, not the criterion's. JAK-38 blocked-by swapped to JAK-44. Deadline wakeup set 15m.
- next:     JAK-44's completion, or the 15-minute deadline check. Rounds used: 2 of 3.

## 2026-09-17 18:27Z · step 11 · ROUND 2 ACCEPTED (JAK-44) · CLOSE-OUT · SCOPE RULING · SPAWN Evaluate (round 2) (JAK-45)

- event:    the round-2 child completed; PR #10. It swept rather than patched: 437 engine probes over every word and adjacent word pair in all eight rooms' `look`, `lookAgain`, `description` and `takeRefusal`, 16 fixes across 13 `nouns` arrays in five files, no prose, no new entry.
- saw:      on the merged story branch (6c1b673): typecheck clean, 46 pass / 0 fail, `eval:reach` PASS 22 of 22, `eval:voice --story the-eighth-stone` PASS 0 findings. The `src/` diff contains **zero** lines outside `nouns:` arrays. Every added noun answers with the right entry, played by me room by room — including both wrong-thing cases (`STONE CHAMBER` and `CHAMBER WALL` at `ford:1099-aa` now answer with the gates and the wall) and the round-1 fixes still holding (`COURSE` → stone, `CHAMBER WALL` at 2099 AA → wall). Five of the sixteen were misses nobody had reported, which is the difference between a sweep and a list. Three rooms were swept and needed nothing, so they are absent from the diff. The child also re-checked all 425 declared nouns for shadowing (0) and named an honest residual: bare fittings and materials inside descriptions, plus singular/plural gaps, which it left and asked me to rule on.
- decision: accept and merge. Rule the residual class **out of scope** for this story, and write the ruling into the round-2 evaluator's brief rather than leaving it to a fourth reader's taste.
- reason:   the fix is accepted because I re-played every added noun and read the diff; the interesting decision is the ruling. Answering nouns that appear only inside another entry's `description` is a regress with no floor — every description names nouns whose descriptions name more — and the writing guide's rule is that every noun **the room mentions** answers, where the room is what `look` prints. The rooms already declare fittings of that kind where the author chose to (`elm`, `withies`, `grit`, `dowels`, `slot`), which makes the rest a coverage preference. Leaving it unruled is the real risk: at `round: 2` of 3, a fourth reader drawing one finding from an unbounded pool would escalate this story to a human over a word like `thwart`. So the brief states the boundary and asks the evaluator to classify each finding in-scope or note, which keeps the standard honest in both directions — a genuine `look` miss is still a FAIL and I said so.
- action:   merged origin/cylocal1/jak-44-… (6c1b673); story branch pushed. JAK-44 closed out 7/7, state Done. JAK-45 "Evaluate (round 2)" created and delegated, carrying the scope ruling, a requirement to derive the sweep independently rather than from round 2's table, the shadowing checks by name, and the `foot`-resolves-differently-per-room judgement call — which is the one thing in this story no harness can check. JAK-38 blocked-by swapped to JAK-45. Deadline wakeup set 15m.
- next:     the round-2 verdict, or the 15-minute deadline check. Rounds used: 2 of 3 — a FAIL here escalates to a human.

## 2026-09-17 18:35Z · step 12 · EVALUATION PASS (JAK-45) · CLOSE-OUT · PR TO MAIN · IN REVIEW

- event:    the third evaluation reported `verdict: PASS` — 22 of 22 reachable, 0 voice findings, 0 in-scope noun misses.
- saw:      a sweep derived from the prose before the evaluator read round 2's table, then backstopped mechanically with 1158 word and word-pair probes across all eight rooms' `look` and `lookAgain`. Every concrete noun answers, and answers with the thing its sentence means; everything left unanswered is a verb, adjective, count, measurement or position. The shadowing checks hold by name, `nouns[0]` is unchanged on all thirteen widened arrays, and the `foot` judgement is right in all six referents — the milestone at 2099 BA and 99 BA, the stone at 99 AA and 2099 AA, the page at 1099 AA, and silent at `east-road:99-aa`, the one place it is a unit. One finding, filed as a Note with both sides argued: `EXAMINE BANK` at the two faces whose `look` means the near bank answers with the far-bank entry, whose description is the map-edge refusal.
- decision: accept the PASS, do not spend the remaining fix round on the `bank` note, open the PR to `main`, and move the story to In Review.
- reason:   every claim re-ran green under me on the story branch, including the six identical READs (one distinct reply across all six ford faces), zero `east:` keys in any ford file, and the eighteen-command round trip at exit 0 — so the close-out ticks are a verification record. The `bank` note is correctly a note: the noun answers, and it answers with a bank, which is not the wrong-*kind*-of-object shape that made `chamber wall`→gates and `stone chamber`→milestone real defects. And `bank` sits on `ford-far-bank-*` deliberately, to guard the edge of the map in the two rooms where a player is likeliest to test whether they can cross; re-pointing it weakens that guard, so it is a trade for a human to make in review, not a fix to spend the last round on. Escalating a PASS over an arguable trade would have been the worse error in both directions.
- action:   JAK-45 closed out 6/6, state Done; blocked-by removed. PR #11 opened `cylocal1/jak-38-the-eighth-stone` → `main` with the log linked and five things named for review: the `bank` note, `east-road:99-aa` as the weakest of the eight (the final evaluator's read, and I agree), the undiscoverable `east` exit at both crossing faces, the game-wide possessive sub-part gap in the previous story's shipped rooms, and the two process defects this run found in ADVENTURE_FACTORY.md §6 and the fix-round criteria.  JAK-38 moved to In Review.
- next:     human review of PR #11. Rooms: 8 of 8. Rounds used: 2 of 3. Revisions used: 0 of 5.
